<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>USERS</title>
  </head>
  <body>

    <ul>
      <% tokiresults.forEach((r) => { %>
        <li><%= r.name %></li>
      <% }); %>

    </ul>
  </body>
</html>
